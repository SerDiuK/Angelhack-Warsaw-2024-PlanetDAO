'use client';

import IdeaDetail from '@/app/features/IdeaDetail';

export default function IdeaDetailPage() {
  return (
    <main className="flex min-h-screen w-full flex-col items-center justify-between">
      <IdeaDetail />
    </main>
  );
}
